/.lake/
/build/

# Artifacts left in the repo root by the radar benchmark suite (scripts/bench)
/measurements.jsonl
/lakeprof.log
/lakeprof.trace_event
/index.html

__pycache__/
*.pyc

# Claude Code agent worktrees are nested checkouts, never repository content.
.claude/worktrees/

# Generated from the root pins by scripts/sync_web_pins.py; never committed, so that a
# Mathlib bump touches only the root pins. See that script and web/examples/lakefile.lean.
web/examples/lake-manifest.json
web/examples/lean-toolchain

# Pinned read-only merge policy used by status tooling and CI
.tauceti-review/
